# 未比對到 server_name 的流量回 nginx 內建 404，不落到任何站台
# h2c preface 偵測只看 default server 的 http2 設定，這裡開了整個 :80 才收得到 gRPC（h2c）
server {
    listen 80 default_server;
    http2 on;
    server_name _;
    return 404;
}
